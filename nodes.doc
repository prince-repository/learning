******Creating a New Repo on GitHub and Adding Files*******
Step 1: Add a new repository on GitHub.

Step 2: List files in your project folder.

$ ls

assets/  index.html  nodes.doc

Step 3: Check hidden files (including .git).

$ ls -a
./  ../  .git/  assets/  index.html  nodes.doc

Step 4: Stage all files for commit.

$ git add .

Step 5: Add the remote repository URL.

$ git remote add origin https://github.com/prince-repository/learning.git

Step 6 (Optional): If you need to change the remote URL, remove the existing one and add the new URL.


$ git remote remove origin
$ git remote add origin https://github.com/prince-repository/learning.git

Step 7: Push your changes to GitHub.

$ git push -u origin main




   *******Steps to Update (Push Updates) to GitHub:********

Step 1: Check for Changes (Optional)
Pehle yeh dekho ki tumne koi changes kiye hain ya nahi:


$ git status
Yeh command tumhe batayegi ki koi files modified hain ya nahi.

Step 2: Stage the Changes
Jo changes tumne kiye hain, unko stage karo.


$ git add .
Ya agar sirf specific file ko stage karna ho, toh:


$ git add <filename>
Step 3: Commit the Changes
Apne staged changes ko commit karo, aur commit message do:


$ git commit -m "Updated the index.html and added new assets"

Step 4: Push the Changes to GitHub
Ab tumhare changes ko GitHub repository pe push karne ke liye yeh command use karo:


$ git push origin main
Agar tum master branch use kar rahe ho, toh main ki jagah master likhna hoga:


$ git push origin master
